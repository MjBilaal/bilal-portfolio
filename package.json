{
  "name": "bilal-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "live-server ./",
    "build": "echo 'No build step needed for static site'",
    "deploy": "gh-pages -d ./"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "gh-pages": "^5.0.0"
  }
}
